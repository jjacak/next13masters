query ProductsGetList ($count:Int!, $offset:Int!){
	products(first: $count, skip: $offset) {
		id
		name
		description
		categories(first: 1) {
			name
		}
		images(first: 1) {
			url
		}
		price
		rating {
			count
			rate
		}
	}
}
